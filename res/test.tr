// SPDX-FileCopyrightText: 2022 Cem Geçgel <gecgelcem@outlook.com>
// SPDX-License-Identifier: GPL-3.0-or-later

var a: int = 200.0e-1;
let b: int = -5.1e2;
let c: int = a + a*b + b;
let d: int = 1*2/(3+4)%5-6;

let pi: double = +3.14;
let e : double = +2.72;
let r : double = pi/r;

var someVariable     : int = 000_017_350_00;
var someOtherVariable: int;

someOtherVariable = someVariable * 2 + 70e+5;

a = func(b, 3, someOtherVariable);
a = func();
a = func(3);

someVariable = a++ * --someOtherVariable;

var nice: int = 7;
nice += 1;    // 8
nice /= 2;    // 4
nice %= 3;    // 1
nice *= 4;    // 4
nice -= -5*6; // 34

nice++;
nice--;

nice += 4;

++nice;
--nice;

func();

var boolean: bool = false;
boolean = !boolean;

someOtherVariable = ~someVariable;

let shifty0 : int = 1 << 4;
let shifty1 : int = shifty0 >> 2;
var shifty2 : int = ~shifty1;
shifty2 <<= 4;
shifty2 >>= 3;

var comp: bool = a < b;
comp = a <= b;
comp = a >= b;
comp = a > b;

comp = c == d;
comp = a < b != a > b;

var shortCircuit: bool = a < b && a > b;
comp ^= shortCircuit;
comp &= shortCircuit;
comp |= shortCircuit;

let MASK : int = 1 << 5;
var res: bool = a & MASK != 1 << 2;
