// SPDX-FileCopyrightText: 2022 Cem Geçgel <gecgelcem@outlook.com>
// SPDX-License-Identifier: GPL-3.0-or-later

let short = [](x: int) 3 * x;

let long: [](int)int = [](x: int) int { return 3 * x; }

let higher = [](x: int, f: [](int)int) f(x);

let main = []() {
  let result0 = higher(3, short);
  let result1 = higher(3, long);

// Closure not implemented.
//  let inline = higher(result, [result](x: int) result * x);
  let inline = higher(result, [](x: int) 5 * x);
}
